import { mollieIntegration } from './mollie-integration';

describe('mollieIntegration', () => {
    it('should work', () => {
        expect(mollieIntegration()).toEqual('mollie-integration');
    })
})